# CPlus - a Sane package manager and build tool for C++

## Bootstrapping

### Required CLI Tools

For bootstrapping the following tools are requied (they are not requied for regular use)

* git
* python3
* pyyaml (Python yaml parsing library)
